# 花笺 Floral Notepaper 项目分析

> 仓库:`Achilng/floral-notepaper` v1.1.0(MIT)
> 定位:轻量、优雅、现代化的本地便签工具
> 技术栈:**Tauri 2 + React 19 + Vite 8 + TypeScript**,Rust 后端(2021 edition),Tailwind CSS 4

---

## 一、项目概况

- **发布渠道**:GitHub Releases / Mirror酱(国内镜像,含 CDK 赞助)/ Microsoft Store(MSIX)
- **平台**:Windows(x64/AArch64)、macOS(Intel/Apple Silicon)、Linux(deb/rpm/AppImage)
- **国际化**:简体中文 / 繁體中文 / English(i18next)
- **工程质量**:前端 vitest 单元测试 20+、Rust 内置单元测试(desktop.rs / notes.rs / updater 均有 test 模块)、husky + lint-staged + oxlint + oxfmt 门禁、CI、测试覆盖迁移/边界场景
- **活跃度**:最近推送 2026-08-24,278 forks,有正式 Release 与更新日志

## 二、功能清单(对照速记软件 readme 需求)

| readme 需求 | 花笺实现 | 状态 |
|---|---|---|
| 可自启动,后台静默 | `tauri-plugin-autostart` + 系统托盘;托盘菜单可开关自启动/关闭到托盘 | ✅ |
| 多种快捷键启动 | 全局快捷键**可自定义 + 可视化录制**,默认 `Ctrl+Space` 呼出小窗;另有"显示/隐藏主窗口"快捷键 | ⚠️ 仅 2 个动作,见差距分析 |
| 强调打开速度 | **便签窗口池 + 预热**(prewarm/pool/recycle),小窗常驻内存秒开;低内存模式 | ✅ 核心亮点 |
| 完成编辑后调外观 | 背景图、便签纸颜色(tileColor)、透明无边框窗口、主题 | ✅(透明度调节粒度有限) |
| 固定桌面,不影响鼠标 | 磁贴模式(Tile):置顶固定在桌面,双击编辑 | ⚠️ 置顶但不穿透鼠标 |
| 桌面堆放多份便签/待办 | 小窗多开(每笔记独立窗口)+ 磁贴;待办用 Markdown 复选框 | ✅ |

**额外功能**:Markdown 编辑/预览(GFM + KaTeX 数学公式 + 告警块)、图片粘贴与清理、.md 导入导出、分类管理、外部 .md 文件编辑(文件关联 .md/.txt,双击打开)、应用内自动更新(含 Mirror酱 CDK)、单实例、CLI(--version/--help)、数据目录迁移。

## 三、架构分析

### 1. 三种窗口 = 三种功能页(URL 路由驱动)
`src/features/windows/windowRoutes.ts` —— 单一前端应用,通过 URL 参数区分窗口用途:

- `index.html?view=main` → 主窗口(笔记列表 + 编辑)
- `index.html?view=notepad&noteId=xxx` → 快捷小窗(快速便签,260×260)
- `index.html?view=tile&noteId=xxx` → 磁贴窗(固定桌面)

### 2. 打开速度的关键:窗口池预热(desktop.rs)
- `schedule_notepad_prewarm` / `prewarm_notepad`:后台预创建小窗
- `activate_pooled_notepad`:唤起时直接从池中取,窗口已加载完成 → **秒开**
- `recycle_notepad_window`:关闭后回收进池复用,而非销毁
- `set_webview_memory_usage_level(window, low)`:不活跃窗口降 WebView 内存占用
- `cursor_centered_bounds`:小窗默认在鼠标位置弹出

### 3. 快捷键系统(desktop.rs)
- `tauri-plugin-global-shortcut`,`ShortcutBindings` 绑定表:`open_notepad`(呼出小窗)+ `toggle_visibility`(显示/隐藏主窗口)
- **录制功能**:`start/stop_shortcut_recording`,录制期间临时注销全部快捷键
- **冲突检测**:`system_shortcut_conflict`(禁与系统 IME 快捷键冲突,含禁用/恢复 IME 热键保护)、占用检测、重复校验(两快捷键不可相同)
- 快捷键格式:`Ctrl+Shift+K`、`Alt+F2` 等,校验严格

### 4. 数据存储(services/notes.rs)—— 纯本地、零数据库
```
数据目录(可自定义,FLORAL_NOTEPAPER_DATA_DIR 环境变量)
├── config.json        # 配置(原子写入 write_json_atomic)
├── metadata.json      # 笔记元数据(id/标题/分类/时间/字数/预览)
├── notes/             # 笔记本体 = 纯 .md 文件,按分类分目录(notes/工作/x.md)
├── images/            # 笔记内嵌图片
└── backgrounds/       # 便签纸背景图
```
- 笔记即 `.md` 文件 → **任何编辑器可读**,天然可备份/迁移
- 完整的旧版迁移逻辑(v1.0.4 布局迁移、损坏 metadata 自动重建并备份、数据目录搬迁)
- 三种自动保存开关:note_auto_save / note_surface_auto_save / external_file_auto_save

### 5. 自研更新器(src-tauri/src/updater/ ~14 个模块)
check / download / install 全链路自研,含 file_lock、CDK store、自动检查调度器、平台差异处理、MSIX(商店版)自动禁用应用内更新。

### 6. 其他细节
- 图片上传走 **raw 二进制直传**(headers 传 noteId/扩展名),避免 JSON 序列化开销 —— 大图粘贴性能优化
- 单实例插件 + 文件关联启动参数提取(take_startup_file / open-external-file)
- 拖拽窗口带偏移补偿(`start_window_drag_with_offset`),配合前端 deadzone 避免拖拽卡顿
- 权限最小化:capabilities 仅授予窗口操作/对话框/剪贴板等必要权限

## 四、与速记软件需求的主要差距(可做的差异化)

1. **多快捷键 → 多功能页映射缺失**(最大差距)
   readme 要求"每种快捷键启动不同功能,如笔记、待办等,一次只开一页"。花笺只有 2 个全局快捷键(呼出小窗 / 显示主窗),且**没有独立的待办功能页**。扩展方案:仿 `ShortcutBindings` 增加动作表(如 `open-todo`),为待办建独立窗口路由(`view=todo`)。
2. **点击穿透缺失**
   磁贴模式是"置顶"但不穿透鼠标。readme 要求"固定桌面不影响鼠标操作"。需加 `set_ignore_cursor_events`(capabilities 需补 `core:window:allow-set-ignore-cursor-events`)。
3. **透明度调节粒度**
   窗口 `transparent: true` 但未发现前台透明度滑杆(仅背景图/便签纸颜色)。
4. **待办独立页**
   当前待办 = Markdown 复选框;若需原生待办(勾选排序、截止、提醒),需新增数据模型。

## 五、对本项目最值得借鉴的实现

| 亮点 | 借鉴方式 |
|---|---|
| 窗口池预热(秒开) | 直接照搬 `prewarm/pool/recycle` 思路,这是"强调打开速度"的最优解 |
| 窗口路由单一前端 | `view` URL 参数区分窗口,一套前端代码维护三种页面 |
| 笔记 = 纯 .md 文件 | 数据即文件,零数据库依赖,导出/备份零成本 |
| 快捷键录制 + 冲突检测 | 现成参考,含系统快捷键(IME)保护逻辑 |
| raw 二进制图片直传 | 大文件性能优化范式 |
| 托盘 + autostart + close-to-tray | 后台静默标准组合,直接对标 readme 第一条 |
