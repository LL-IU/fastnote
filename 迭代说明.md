# fastnote 迭代说明

> 本文件按版本详细记录 fastnote 的全部修改记录。
> 版本号规则：`主版本.次版本.修订`——初版定制为 0.1.0，后续每次功能/修复修订递增修订号。

---

## 0.1.2（当前版本）

- **状态**：当前版本（2026-08）
- **主题**：WebDAV（坚果云）同步新功能、转磁贴/磁贴默认不再置顶、主题文字纯黑/纯白、小窗顶栏布局修复、数据目录子文件夹改名 fastnotes、显示/隐藏窗口快捷键联动磁贴、多项 WebDAV 修复
- **涉及文件**：
  - 前端：`src/components/NotePad.tsx`、`src/App.css`、`src/components/SettingsPanel.tsx`、`src/components/MainWindow.tsx`、`src/features/settings/webdav.ts`、`src/features/notes/api.ts`、`src/locales/{zh-CN,zh-HK,en-US}/translation.json`
  - 后端：`src-tauri/src/services/webdav.rs`（新）、`src-tauri/src/services/notes.rs`、`src-tauri/src/services/mod.rs`、`src-tauri/src/desktop.rs`、`src-tauri/src/lib.rs`、`src-tauri/Cargo.toml`
  - 版本：`package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` → **0.1.2**
- **验证**：前端 `tsc + vite build` 通过；`npm test` 75/75 通过；`cargo check` 通过；`tauri build --no-bundle` 产出便携版 exe；冒烟测试启动/退出正常

### 一、转磁贴默认不再置顶（行为调整）

- 背景：0.1.1 实现置顶/置底继承时，「默认」状态转磁贴会强制置顶（保持"钉桌面"语义）。
- 修改：`applyPinState` 去掉 `defaultAlwaysOnTop` 参数，「none」状态**不再调用** `setAlwaysOnTop` / `set_window_bottom`，转磁贴 / 转回小窗均**不改变窗口层级**。

| 小窗设置 | 转为磁贴后 | 再转回小窗 |
|---|---|---|
| 置顶 | 磁贴置顶 | 恢复置顶 |
| 置底 | 磁贴置底（取消置顶 + HWND_BOTTOM） | 恢复置底 |
| 默认（未设置） | **不改变窗口层级**（不再自动置顶） | 保持现状 |

### 二、主题文字颜色（`src/App.css`）

- 浅色模式主文字：`#1a1a18` → **纯黑 `#000000`**
- 深色模式主文字：`#e5e1da` → **纯白 `#ffffff`**
- 仅改 `--color-ink`（主文字 token）；图标使用 `ink-ghost/faint/soft`，颜色不变。

### 三、小窗顶栏布局修复（`src/components/NotePad.tsx`）

- 问题：小窗最小宽度 220px，旧顶栏需约 246px，右侧按钮溢出、关闭按钮被遮挡一半。
- 修复：新建/打开按钮缩小（`px-2.5 py-1 text-[11px]`）、容器内边距 `px-4 → px-2.5`、右侧图标组间距 `gap-1.5 → gap-1`、两组 `shrink-0` + 标签 `whitespace-nowrap`。修复后占用约 208px ≤ 220px。

### 四、WebDAV 同步（坚果云）—— 新功能

**后端 `src-tauri/src/services/webdav.rs`（新模块）**
- 同步对象：`metadata.json` + `notes/` 下全部笔记 .md 文件，打包为单文件 `fastnote.json` 上传到远端
- 双向同步按文件路径 + 修改时间合并（last-write-wins）；「从云端恢复」下载覆盖本地
- 密码 AES-GCM 随机 nonce 加密后存入 `<config_dir>/webdav.json`（非明文）
- 命令：`webdav_set_config` / `webdav_get_config` / `webdav_test` / `webdav_sync_now` / `webdav_restore` / `webdav_status`
- 后台轮询：启用后每 5 分钟自动双向同步（`start_webdav_sync_poll`）
- 新增依赖：`reqwest`（rustls）、`aes-gcm`、`base64`、`rand`、`tokio`（time）、`http`

**前端**
- `src/features/settings/webdav.ts`：命令封装
- `SettingsPanel.tsx` 新增「WebDAV 同步（坚果云）」设置区：启用开关、服务器地址 / 账号 / 应用密码 / 远端路径、测试连接 / 立即同步 / 从云端恢复、上次同步时间与状态提示
- 三语 i18n（zh-CN / zh-HK / en-US）新增 `settings.webdav.*` 键

**使用说明**：服务器地址 `https://dav.jianguoyun.com/dav/`，账号为坚果云邮箱，密码为网页控制台生成的「应用密码」（不是登录密码）。

### 五、数据目录子文件夹改名 floral → fastnotes

- `lib.rs config_migrate_data_dir`：重定向数据目录时创建的子文件夹由 `floral` 改为 **`fastnotes`**
- `MainWindow.tsx` 迁移确认文案同步更新（`settings.dataDir.confirmSubdir`）
- 环境变量 `FLORAL_NOTEPAPER_*` 向后兼容保留（仅用于读取旧版配置）

### 六、磁贴默认不再置顶（Rust 修复）

- Rust 独立磁贴窗 `open_tile_window_now` 的 `always_on_top` 由 `true` 改为 `false`
- 结合前端「默认状态不改变层级」，磁贴在任何入口下默认都不置顶；置顶/置底仅由用户显式操作

### 七、显示/隐藏窗口快捷键联动磁贴

- 功能描述（设置面板 + 三语）由「显示/隐藏窗口快捷键」改为「**打开主窗口并打开桌面磁贴**」
- `toggle_app_visibility` 重写：隐藏时只隐藏主窗口 + 磁贴窗口；打开时打开主窗口并重新显示已钉在桌面的磁贴（恢复时确保主窗口一并显示）

### 八、WebDAV 设置页输入框样式修复

- 问题：输入框使用 `flex-1` 但无 flex 父容器 → 不撑满宽度、观感异常。
- 修复：`min-w-0 flex-1` → `w-full`，并与上方其他设置项一致使用背景 `bg-paper-warm/70` + 边框。

### 九、WebDAV 首次同步 409 修复

- 问题：坚果云对「父目录不存在」的资源请求返回 **409**，首次同步时远端 `/fastnote` 目录尚未创建，GET 报「下载失败，服务器返回 409 Conflict」。
- 修复：
  1. 同步前**先 MKCOL 创建远端目录**（`ensure_remote_dir` 移到 GET 之前）
  2. GET 时 **404 与 409 均视为「远端无文件」**，直接上传本地
  3. 「从云端恢复」遇 404/409 时提示「远端文件不存在，请先执行立即同步」

### 十、删除快捷键下方描述

- `ShortcutRecorder` 默认提示消息（「用于打开快捷记录小窗」）改为空，快捷键下方不再显示默认描述文字
- 清理三语中不再使用的 `settings.shortcut.forQuickNote` 键；冲突/检测等反馈提示保留

### 产物

- 便携版可执行文件：`速记软件\fastnote.exe`（release 构建，免安装）

---

## 0.1.1（已发布）

- **状态**：已发布（2026-08）
- **主题**：小窗 ↔ 磁贴的置顶 / 置底状态继承 + 便携版 exe 发布
- **涉及文件**：`src/components/NotePad.tsx`
- **验证**：前端 `tsc + vite build` 通过；`npm test` 75/75 通过；`tauri build --no-bundle` 产出便携版 exe；冒烟测试启动/退出正常

### 背景与问题

小窗（快捷便签）支持置顶 / 置底，磁贴定位为「固定桌面」。但 **小窗 → 磁贴转换时，原代码强制 `setAlwaysOnTop(true)`**：

1. 小窗设了**置底**后转磁贴 → 被强制变回**置顶**，用户设置的置底状态丢失；
2. 原状态只有 `pinnedTop` 一个布尔，**无法区分「默认」与「置底」**；
3. 磁贴 → 转回小窗时不恢复小窗的钉状态，窗口停留在磁贴时的层级，与 UI 显示脱节。

### 详细修改

1. **三态钉状态**：新增 `pinnedBottom` state，与 `pinnedTop` 互斥，构成「置顶 / 置底 / 默认」三态。
2. **统一的层级应用函数 `applyPinState(pin)`**：`"top"` → 置顶；`"bottom"` → 取消置顶 + 置于 Z 序最底（HWND_BOTTOM）；`"none"` → 默认。
3. **`switchSurfaceMode` 双向同步**：转磁贴沿用小窗钉状态，转回小窗恢复钉状态。
4. **磁贴兜底 effect**：进入磁贴模式时按钉状态设置层级（双保险，幂等）。
5. **按钮互斥与反馈**：置顶 / 置底互斥清空另一状态；置底按钮新增激活高亮；加载配置时重置 `pinnedBottom`。

### 行为对照

| 小窗设置 | 转为磁贴后 | 再转回小窗 |
|---|---|---|
| 置顶 | 磁贴置顶 | 恢复置顶 |
| 置底 | 磁贴置底（取消置顶 + HWND_BOTTOM） | 恢复置底 |
| 默认（未设置） | 发布时默认置顶（0.1.2 起调整为不改变层级，见上） | — |

> 说明：Windows 无「永久置底」API，置底为一次性 `SetWindowPos(HWND_BOTTOM)`，与现有小窗置底行为一致；点击窗口时会被系统重新置前。

### 产物

- 便携版可执行文件：`速记软件\fastnote.exe`（release 构建，免安装）

---

## 0.1.0（初版：基于 floral-notepaper 深度定制）

- **状态**：初始版本（2026-08）
- **来源**：上游开源项目 [floral-notepaper（花笺）](https://github.com/Achilng/floral-notepaper) v1.1.0（MIT）
- **定位**：快速启动、后台静默的 Windows 桌面便签软件

### 一、更名与品牌

- 应用名：`floral-notepaper / 花笺` → **`fastnote`**
- 版本号重置：`v1.1.0` → **`0.1.0`**
- 标识符：`com.fastnote.app`；窗口标题 / 托盘提示 / 菜单文案全部替换为 fastnote
- 移除上游品牌文档、贡献者展示、Logo 相关宣传物料

### 二、移除的功能（与本项目定位无关）

| 功能 | 说明 |
|---|---|
| 应用内更新 / 自动更新 | 移除整个更新器模块（check/download/install）及相关依赖 |
| Mirror酱 CDK | 移除国内镜像分发与赞助通道 |
| Microsoft Store / MSIX 分发 | 移除商店版打包与分发配置 |
| 跨平台打包 | 打包目标收敛为 Windows（bundle.targets = `["app"]`）；macOS/Linux 源码分支保留但不再打包分发 |

### 三、新增 / 调整的功能

| 项目 | 说明 |
|---|---|
| **小窗默认不再置顶** | 新增设置项「小窗默认置顶」（`notepadAlwaysOnTop`，默认 `false`），默认行为由「置顶」改为「不置顶」 |
| **小窗置顶 / 置底按钮** | 小窗右上角新增 ↑ 置顶、↓ 置底按钮，一键切换 Z 序层级 |
| **磁贴行为设置** | 保留并整理磁贴相关设置：`tileCtrlClose`（Ctrl+关闭保护）、`tileDoubleClickToEdit`（双击进入编辑）、`tileSaveReturnsToPin`（保存后返回磁贴）、`tileRenderMarkdown`（磁贴渲染 Markdown） |
| **数据目录重定向** | 环境变量 `FASTNOTE_DATA_DIR`（兼容旧变量名 `FLORAL_NOTEPAPER_DATA_DIR`），数据默认放 `文档\fastnote` |
| **便携版发布** | `tauri build --no-bundle` 产出免安装 `fastnote.exe` |
| 文件关联 | 保留 `.md` / `.txt` 文件关联（双击用 fastnote 打开） |
| CLI | 保留 `--version` / `--help` 输出 |

### 四、保留的核心能力（上游亮点原样继承）

- **小窗窗口池预热**：预创建 + 回收复用 + 低内存档位，唤起秒开
- **全局快捷键录制**：自研 WH_KEYBOARD_LL 键盘钩子 + IME 热键保护/恢复 + 冲突检测
- **Markdown 编辑/预览**：GFM + LaTeX（KaTeX）+ 告警块
- **磁贴模式**：固定桌面，桌面可堆放多份
- **纯 `.md` 文件存储**：笔记即文件，零数据库，可任意编辑器打开
- **图片粘贴**：raw 二进制直传 + 无用图片清理
- **三语 i18n**：简体中文 / 繁體中文 / English（前后端一致）
- **工程质量**：前端 vitest 20 个测试文件 + Rust 35+ 内嵌测试 + husky/oxlint/oxfmt 门禁

### 五、已知遗留与后续处理

| 遗留项 | 状态 |
|---|---|
| `config_migrate_data_dir` 硬编码 `floral` 子目录 | ✅ 已在 0.1.2 改为 `fastnotes` |
| capabilities 中 `opener:allow-open-url` 仅放行 `ms-windows-store://*`（死权限） | ⏳ 未处理 |
| 标识符 `com.fastnote.app` 以 `.app` 结尾（Tauri 构建警告） | ⏳ 未处理 |
| 未建立 git 仓库 | ✅ 已建立并推送 GitHub（`LL-IU/fastnote`，main 分支） |
