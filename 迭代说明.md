# fastnote 迭代说明

> 本文件按版本详细记录 fastnote 的全部修改记录。
> 版本号规则：`主版本.次版本.修订`——初版定制为 0.1.0，后续每次功能/修复修订递增修订号。

---

## 0.1.1（当前版本）

- **状态**：当前版本（2026-08）
- **主题**：小窗 ↔ 磁贴的置顶 / 置底状态继承 + 便携版 exe 发布
- **涉及文件**：`src/components/NotePad.tsx`（唯一源码改动）
- **验证**：前端 `tsc + vite build` 通过；`npm test` 75/75 通过；`tauri build --no-bundle` 产出便携版 exe；冒烟测试启动/退出正常

### 背景与问题

小窗（快捷便签）支持置顶 / 置底，磁贴定位为「固定桌面」。但 **小窗 → 磁贴转换时，原代码强制 `setAlwaysOnTop(true)`**：

1. 小窗设了**置底**后转磁贴 → 被强制变回**置顶**，用户设置的置底状态丢失；
2. 原状态只有 `pinnedTop` 一个布尔，**无法区分「默认」与「置底」**；
3. 磁贴 → 转回小窗时不恢复小窗的钉状态，窗口停留在磁贴时的层级，与 UI 显示脱节。

### 详细修改

1. **三态钉状态**
   - 新增 `pinnedBottom` state，与 `pinnedTop` 互斥，构成「置顶 / 置底 / 默认」三态（两者皆否为默认）。

2. **统一的层级应用函数 `applyPinState(pin, defaultAlwaysOnTop)`**
   - `"top"` → 置顶（`setAlwaysOnTop(true)`）
   - `"bottom"` → 取消置顶 + 置于 Z 序最底（`setAlwaysOnTop(false)` + `set_window_bottom` / HWND_BOTTOM）
   - `"none"`（默认）→ 按上下文：磁贴模式置顶（保持「钉桌面」语义），小窗模式不置顶

3. **`switchSurfaceMode` 双向同步**
   - 转磁贴：沿用小窗当前钉状态（置顶→磁贴置顶；置底→磁贴置底；默认→磁贴置顶）
   - 转回小窗：恢复小窗钉状态（置顶→恢复置顶；置底→恢复置底；默认→不置顶）

4. **磁贴兜底 effect**
   - 进入磁贴模式时按钉状态设置层级（双保险，幂等）。

5. **按钮互斥与反馈**
   - 置顶 / 置底按钮操作时互斥清空另一状态；
   - 置底按钮新增激活高亮（与置顶按钮一致）；
   - 加载配置 / 设置变更「小窗默认置顶」时重置 `pinnedBottom`。

### 行为对照

| 小窗设置 | 转为磁贴后 | 再转回小窗 |
|---|---|---|
| 置顶 | 磁贴**置顶** | 恢复**置顶** |
| 置底 | 磁贴**置底**（取消置顶 + HWND_BOTTOM） | 恢复**置底** |
| 默认（未设置） | 磁贴**置顶**（保持钉桌面语义） | 不置顶不置底 |

> 说明：Windows 无「永久置底」API，置底为一次性 `SetWindowPos(HWND_BOTTOM)`，与现有小窗置底行为一致；点击窗口时会被系统重新置前。

### 产物

- 便携版可执行文件：`速记软件\fastnote.exe`（release 构建，约 26 MB，免安装）

---

## 0.1.0（初版：基于 floral-notepaper 深度定制）

- **状态**：初始版本（2026-08）
- **来源**：上游开源项目 [floral-notepaper（花笺）](https://github.com/Achilng/floral-notepaper) v1.1.0（MIT）
- **定位**：快速启动、后台静默的 Windows 桌面便签软件

### 一、更名与品牌

- 应用名：`floral-notepaper / 花笺` → **`fastnote`**
- 版本号重置：`v1.1.0` → **`0.1.0`**
- 标识符：`com.fastnote.app`；窗口标题 / 托盘提示 / 菜单文案全部替换为 fastnote
- 移除上游品牌文档、贡献者展示、Logo 相关宣传物料

### 二、移除的功能（与本项目定位无关）

| 功能 | 说明 |
|---|---|
| 应用内更新 / 自动更新 | 移除整个更新器模块（check/download/install）及相关依赖 |
| Mirror酱 CDK | 移除国内镜像分发与赞助通道 |
| Microsoft Store / MSIX 分发 | 移除商店版打包与分发配置 |
| 跨平台打包 | 打包目标收敛为 Windows（bundle.targets = `["app"]`）；macOS/Linux 源码分支保留但不再打包分发 |

### 三、新增 / 调整的功能

| 项目 | 说明 |
|---|---|
| **小窗默认不再置顶** | 新增设置项「小窗默认置顶」（`notepadAlwaysOnTop`，默认 `false`），默认行为由「置顶」改为「不置顶」 |
| **小窗置顶 / 置底按钮** | 小窗右上角新增 ↑ 置顶、↓ 置底按钮，一键切换 Z 序层级 |
| **磁贴行为设置** | 保留并整理磁贴相关设置：`tileCtrlClose`（Ctrl+关闭保护）、`tileDoubleClickToEdit`（双击进入编辑）、`tileSaveReturnsToPin`（保存后返回磁贴）、`tileRenderMarkdown`（磁贴渲染 Markdown） |
| **数据目录重定向** | 环境变量 `FASTNOTE_DATA_DIR`（兼容旧变量名 `FLORAL_NOTEPAPER_DATA_DIR`），数据默认放 `文档\fastnote` |
| **便携版发布** | `tauri build --no-bundle` 产出免安装 `fastnote.exe` |
| 文件关联 | 保留 `.md` / `.txt` 文件关联（双击用 fastnote 打开） |
| CLI | 保留 `--version` / `--help` 输出 |

### 四、保留的核心能力（上游亮点原样继承）

- **小窗窗口池预热**：预创建 + 回收复用 + 低内存档位，唤起秒开
- **全局快捷键录制**：自研 WH_KEYBOARD_LL 键盘钩子 + IME 热键保护/恢复 + 冲突检测
- **Markdown 编辑/预览**：GFM + LaTeX（KaTeX）+ 告警块
- **磁贴模式**：恒置顶固定桌面，桌面可堆放多份
- **纯 `.md` 文件存储**：笔记即文件，零数据库，可任意编辑器打开
- **图片粘贴**：raw 二进制直传 + 无用图片清理
- **三语 i18n**：简体中文 / 繁體中文 / English（前后端一致）
- **工程质量**：前端 vitest 20 个测试文件 + Rust 35+ 内嵌测试 + husky/oxlint/oxfmt 门禁

### 五、已知遗留（0.1.1 未处理，留待后续）

- `config_migrate_data_dir` 仍硬编码创建 `floral` 子目录，迁移确认文案含「floral」字样（花笺旧名残留）
- capabilities 中 `opener:allow-open-url` 仅放行 `ms-windows-store://*`，但前端已无商店相关代码（死权限）
- 标识符 `com.fastnote.app` 以 `.app` 结尾，Tauri 构建有警告（建议改为 `com.fastnote.desktop` 之类）
- 未建立 git 仓库（无版本历史，无法对比上游/回滚）
